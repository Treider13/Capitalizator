# Аудит кода 2026-09-02: что было, что сделано, что осталось

**Статус документа:** реализовано (ссылки на модули и тесты ниже). Живой ленты, testnet-hello и биржевых fills в этом репозитории по-прежнему **нет** — среда без egress на `api.bybit.com` (403), всё биржевое проверено на фейковой сессии с документированными формами ответов v5 и на подписи pybit (`tests/gateway/test_pybit_signing.py`).

## Найдено и исправлено (по коду, не по описанию)

| # | Дефект | Где было | Исправление | Тесты |
|---|---|---|---|---|
| D-01 | `REJECT` ≡ `FailedBreak.tag` → идея `failed_break` в `_BREAK_IDEAS` → **шорт удержанной поддержки**, жюри голосовало бычьими голосами за шорт | `desk/loop.py`, `exec/strategy_bounce.py` | `exec/ideas.py`: `bounce` / `spring` (с зоной, стоп за фитилём) / `breakout`; `fade_spring` — только теневой претендент | `tests/exec/test_ideas.py`, `tests/desk/test_orchestrator.py`, `tests/desk/test_exchange_sync.py` (шорт-спринг у сопротивления) |
| D-02 | Теневой R = ±1 по тегу; для старого шорта знак был обратный | `champion/shadow_day.py` | `side_r` по стороне интента; `row_r`; paper-R рядом | `test_ideas.py::test_row_r_prefers_sides…` |
| D-03 | `n≥20` — частота метки, не предиктивность | `jury/desk.py` | `champion/calibrate.py`: Wilson-ДИ по filled shadow paper; класс с верхней границей < break-even **не отправляется** live, тень продолжает | `tests/champion/test_calibrate.py`, `test_exchange_sync.py::test_refuted_class…` |
| D-04 | Один `tick=0.1` на 24 монеты | `desk/loop.py` | `instruments/registry.py`: тик/лот/minNotional по символу (`instruments-info`), неизвестный символ **отказ**, снимок от signer → desk | `tests/instruments/test_registry.py`, `test_exchange_sync.py::test_instruments_published…` |
| D-05/§6 | Стоп = 8 тиков за полосой | `strategy_bounce.py` | `exec/smart_stop.py`: структурный уровень + k·ATR/3·спред + уход от круглых чисел и краёв зон (Osler 2005) | `tests/exec/test_trail_and_smart_stop.py` |
| D-06 | Комиссия 44R, скринер сравнивал с 1% | — | `exec/ev_gate.py`: `1R ≥ fee_multiple_min × (fees+funding+slip)`, `r_net`, break-even winrate; в журнал | `tests/risk/test_account_wiring.py`, `test_orchestrator.py::test_play_100…` (EV-отказ фикстуры) |
| D-08/D-15 | `funding_extreme`, `delisted` — константы | `desk/loop.py` | 95-й перцентиль собственного \|rate\| (n≥20); статус инструмента | `tests/desk/test_screener_inputs.py` |
| D-09/10/11/13/16 | `RiskEngine.on_open/on_flat`, `Halts.update`, `Sizer` не вызывались; `qty=0.001`; бюджет 3 на жизнь процесса | `risk/*` | `risk/account.py` (эквити, открытые идеи, краны, бюджет по сессии МСК, персистентно); `size_position` → `Intent.qty/lev`; signer отказывает unsized | `tests/risk/test_account_wiring.py` |
| D-12 | Меню риска | — | `risk/config.py` + `POST /api/risk` (ack, хеш-звено), hot-reload в desk | `tests/ops/test_console_money.py` |
| D-18 | B-гейт убивал касание до меток (тень голодала) | `desk/loop.py` | Метки ZLG/CAV/лента/PRS всегда; B — причина skip | `tests/desk/test_b_bus.py` |
| D-19 | ICT-метки (fib/FVG/sweep) обязательны, документы зовут их «мусором»; в виртуальных 26 ч резали 169/171 | `card/live.py` | `require_ict_marks` (по умолчанию выкл): метки пишутся (`b_marks_red`, `shadow_would_marks`) | `test_b_bus.py::test_require_ict_marks_knob…` |
| D-20 | Fib только от хая → шорт невозможен | `card/live.py` | `fib_zone_short`, `context_ok(side)` | `tests/card/test_fib_side.py` |
| D-21 | Карта TTL 60 с при публикации раз в 4 ч | `desk/loop.py` | Обновление на рабочем закрытии, если устарела | `test_b_bus.py` |
| D-22 | Blackout 14:00 ET на дне CPI (CPI в 08:30 ET) | `news_macro/rules.py` | `event_windows` по классам из `time.yaml` (BLS/BEA/Fed) | `tests/news/test_event_windows.py` |
| D-25…D-31 | Квадратичный `closed_bars_from_trades`, буферы без границ, коммит SQLite на принт, перечитывание parquet, пересборка зон на принт | `desk/*`, `recorder/sink_parquet.py` | `BarBuilder`, окна буферов, батч-meta, `TapeCursor`, кэш зон, `BufferedParquetSink` (part-файлы), `recorder/rows.py` (без `import pytz` на значение) | `tests/desk/test_bars_builder.py`, `test_perf_bounds.py` |
| D-32…D-37 | UI без эквити/просадки/позиций/статусов | `ops/console.py` | `ops/account_view.py`: `/api/account`, `/api/queue`, `/api/paper`, `/api/risk`, `/api/command`; баннеры «нет данных» с возрастом | `tests/ops/test_console_money.py` |
| D-38 | Вход по цене касания возрастом до 15 мин | — | `valid_until` на интенте; gateway отказывает просроченные | `tests/gateway/test_gateway.py::test_send_refuses_stale…` |
| D-40/Н-4/Н-5 | Dead-man бил сам себя; DCP только для институционалов | `signer/process.py` | `gateway/watchdog.py`: heartbeat стола + private WS; отменяет **входы**, стопы не трогает; тот же сторож в no-key цикле | `test_gateway.py::test_watchdog…`, `::test_legacy_no_key_loop…` |
| D-44/Н-1/Н-2 | Сокета не было; `recv_ts` один на прогон | `recorder/live.py` | `recorder/live_ws.py` на pybit: `recv_ts` на кадр, чанки по 10 подписок, REST-ресинк с уровнями | `tests/recorder/test_live_ws.py` |
| D-46…D-48 | `send = not_sent`, схема testnet-only, flatten локальный | `signer/*` | `gateway/bybit.py` (pybit): post-only лимит с прикреплённым SL (MarkPrice), `orderLinkId`-идемпотентность, `trading-stop`/трейлинг/half-TP/flatten/hello; `PositionTracker` + reconcile; OMS-очередь desk→gateway | `tests/gateway/test_gateway.py` |
| §5.6 | Тень = ±1 по тегу | — | `exec/paper.py`: fill через лимит по ленте, 50%@1R, стоп-маркет с проскальзыванием, TP-лимит, фандинг по расчётам, MAE/MFE, `r_net`; демо-двойник двигает счёт | `tests/exec/test_paper.py`, `tests/desk/test_paper_desk.py` |
| §7 | Трейла не было (`trail_stop` — тождество без вызовов) | `exec/manage.py` | `exec/trail.py`: структурный трейл после +1R, биржевой trailing на импульсе, soft-exit по закрытию; монотонность | `test_trail_and_smart_stop.py` (в т.ч. V-шпиль), `test_paper_desk.py` |

## Виртуальное развёртывание (в среде)

26 ч синтетической ленты BTC+ETH → `LiveRecorder` (фейковый WS) → parquet → desk по курсору → бумага → консоль: 120 630 событий за 55 с (~1600× быстрее реального времени), 171 касание — все с метками, тень исполнена на бумаге с комиссиями, детерминированно между прогонами. Синтетическая лента — случайное блуждание: отрицательный результат тени на ней **ожидаем** и ничего не говорит о рынке.

## Что осталось (честно)

- Живой WS-час и testnet-hello — нужен VPS с egress и ключ (Cloud Agents → Secrets).
- Калибровка `k_atr`, `γ`, порогов ZLG/eaten по MAE — механизм есть (`calibrate.py`, `paper_trades`), данных нет.
- `whales`, `sentiment`, `authors` — интерфейсы без источников данных (как и было).
- Одно касание на зону за 6 ч (D-45) — не менялось.
- Live-fills из private WS пока не заменяют бумажного двойника; сверка идёт через `exchange_state` (REST-правда каждые 60 с): эквити, venue-flat, unknown_position.
