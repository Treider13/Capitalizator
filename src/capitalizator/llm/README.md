# llm

Вход: вечерний отчёт (текст).
Выход: `{summary, trade_advice: false}`. Сокеты запрещены.
Не пишет «долей» / `average_in`. Не делает: ордер, «купи», вызов модели по сети, чтение ключей.
Ключи не читает.
