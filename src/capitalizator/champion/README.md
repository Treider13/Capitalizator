# champion

Вход: синтетический ряд ошибок 0/1; журнал касаний; ночная ширина зоны 0.8/1.2.
Выход: Page-Hinkley `drift`; `shadow_day` считает R тени из исхода; отчёт двух ширин без ордеров. `promote()` — отказ.
Пустой день → `r_shadow` / `r_challenger` NULL, не ноль.
Претендент — выход из COMPRESS (HTF с нами, THROUGH или eaten+RETREAT), не фильтр чемпиона и не очередь.
Pending после рестарта читается из журнала + зоны. Шов в A — после экзамена.
Не делает: смену чемпиона, live-ордер, экзамен 15 дней.
Ключи не читает.
