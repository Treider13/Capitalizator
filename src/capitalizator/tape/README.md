# tape

Вход: сделка с полем side; готовые книги для OFI; книга до касания + лента окна для eaten.
Выход: taker buy/sell; CKS `OFI.window`; `tape_eaten` (≥50% `depth_near` на стороне зоны за 8 с).
Не делает: CVD за 5 минут, выдуманную сторону, вход.
Ключи не читает.
