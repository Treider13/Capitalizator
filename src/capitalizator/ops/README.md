# ops

`check_parquet_count`: путь к parquet и ожидаемое число строк. Код 0 только при точном совпадении.

`check_uptime`: span сделок ≥ `--hours` и нет тишины длиннее `--max-unmarked-gap-s` без события `gap`. Не утверждает, что сутки на VPS уже сняты.

Не делает: торговлю, чтение ключей.
