# news_macro

Вход: `infra/calendars/macro.csv` (даты BLS/Fed, две метки времени).
Выход: строки `news`; срез до `known_at` пустой.
Не делает: скрейп TG, «купи заголовок», коэффициент реакции с потолка.
Ключи не читает.
