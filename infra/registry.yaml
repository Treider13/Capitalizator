# Frozen until the end of phase 1. Numbers from PHASE-BUILD. Do not tune.
epsilon_ticks: 2
bounce_away_ticks: 8
working_tf: "15m"
# Mid TF between the 15m vote and H4. Senior levels pair with the next TF down.
mid_tf: "1h"
htf: "4h"
htf_d1: "1d"
mid_band_ticks: 2
touch_pending_timeout_h: 6
die_no_touch_h: 24
zlg_window_s: 8
zlg_gamma: 0.25
prs_alpha: 0.7
prs_t_max_s: 8
prs_delta_ticks: 5
# Enough observations (one place for jury / ОКО / calibration / first fact).
n_min: 20
mature_n: 30
# Wall (book/wall_watch): before the symbol's Passport is mature, one level worth this
# many USDT; once mature, one level ≥ wall_depth_mult × the symbol's median zone-side depth.
wall_min_notional: 3000000
wall_depth_mult: 1
