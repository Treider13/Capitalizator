# Публичные SSH-ключи операторов

Только строки `ssh-ed25519 …` / `ssh-rsa …`. **Секретный** файл `id_ed25519` сюда не класть.

`install.sh` после клона репозитория дописывает каждый `*.pub` в `/home/trader/.ssh/authorized_keys`. Повторный запуск не дублирует строку.
